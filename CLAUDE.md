# Project conventions

- Credentials are read from a `.env` file. Commit a `.env.example` template, never the `.env` itself. This is the default for all of this user's projects unless told otherwise.
- `scripts/jira_clone_epic.py` clones Jira issues under an epic; see `scripts/README.md`.
