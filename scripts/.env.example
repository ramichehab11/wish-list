# Copy to .env next to this script (or in the directory you run it from)
# and fill in the values. .env is ignored by git.
JIRA_BASE_URL=https://jira.example.com
JIRA_TOKEN=your-personal-access-token
